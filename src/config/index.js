const Config = {
    sideBarLevel: 3, // 侧边栏层级限制, 3表示三级, 可设置 2 和 3
    defaultRoute: '/login', // 默认打开的路由
    // baseUrl: 'http://127.0.0.1:3000',
    baseUrl: 'http://lindom.top',
    notLoginRoute: ['login','home'], // 无需登录即可访问的路由 name
}

export default Config